package com;

import java.util.*;


public class Solution {

    /**
     * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
     * <p>
     * 返回集合大小的最小值
     *
     * @param n long长整型
     * @return int整型
     */
    public int getSize(long n) {
        // write code here
        if (n == 1) {
            return 1;
        }
        for (int i = 1; i < n; i++) {

        }
        return 1;
    }
}